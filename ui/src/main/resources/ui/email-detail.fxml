<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1" 
            fx:controller="com.todo.desktop.ui.controller.EmailDetailController"
            style="-fx-background-color: transparent;"
            stylesheets="@styles.css">
    
    <!-- Header with Back Button -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="16" style="-fx-padding: 0 0 16 0;">
            <Button fx:id="backButton" text="←" onAction="#onBack" styleClass="button-icon">
                <tooltip><Tooltip text="Quay lại"/></tooltip>
            </Button>
            <Label text="Chi tiết Email" styleClass="header-medium" style="-fx-text-fill: -fx-text-muted;"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="statusLabel" styleClass="text-caption"/>
        </HBox>
    </top>
    
    <!-- Content -->
    <center>
        <ScrollPane fitToWidth="true" fitToHeight="true" style="-fx-background-color: transparent;" styleClass="scroll-pane">
            <VBox spacing="20" style="-fx-padding: 0 16 0 0;"> <!-- Right padding for scrollbar -->
                
                <!-- Email Card -->
                <VBox spacing="24" styleClass="card">
                    
                    <!-- Subject -->
                    <Label fx:id="subjectLabel" wrapText="true" styleClass="header-large"/>
                    
                    <!-- Sender Info -->
                    <HBox alignment="CENTER_LEFT" spacing="16">
                        <StackPane prefWidth="48" prefHeight="48" styleClass="avatar-circle">
                            <Label fx:id="senderAvatarLabel" text="A" styleClass="avatar-text"/>
                        </StackPane>
                        <VBox spacing="2" HBox.hgrow="ALWAYS">
                            <Label fx:id="senderNameLabel" text="Sender Name" styleClass="header-medium"/>
                            <Label fx:id="senderEmailLabel" text="sender@example.com" styleClass="text-muted"/>
                        </VBox>
                        <VBox spacing="2" alignment="TOP_RIGHT">
                            <Label fx:id="timeLabel" text="10:30 AM" styleClass="text-caption" style="-fx-font-weight: bold;"/>
                            <Label fx:id="dateLabel" styleClass="text-caption"/>
                        </VBox>
                    </HBox>
                    
                    <!-- To Recipients -->
                    <HBox spacing="8" alignment="CENTER_LEFT">
                        <Label text="Tới:" style="-fx-font-weight: 600;" styleClass="text-muted"/>
                        <Label fx:id="toLabel" wrapText="true" styleClass="text-body"/>
                    </HBox>
                    
                    <Separator style="-fx-background-color: #E2E8F0;"/>
                    
                    <!-- Body Content -->
                    <VBox spacing="16" style="-fx-min-height: 200;">
                        <Label fx:id="bodyLabel" wrapText="true" styleClass="text-body" style="-fx-line-spacing: 6px;"/>
                    </VBox>
                    
                    <!-- Attachments Section -->
                    <VBox fx:id="attachmentsSection" spacing="12" visible="false" managed="false">
                        <Separator style="-fx-background-color: #E2E8F0;"/>
                        <Label text="Tệp đính kèm" styleClass="header-medium" style="-fx-font-size: 14px;"/>
                        <VBox fx:id="attachmentsList" spacing="8"/>
                    </VBox>
                    
                </VBox>
                
            </VBox>
        </ScrollPane>
    </center>
    
    <!-- Bottom Actions -->
    <bottom>
        <HBox spacing="16" alignment="CENTER_LEFT" style="-fx-padding: 16 0 0 0;">
            <Button fx:id="replyButton" text="↩ Trả lời" onAction="#onReply" styleClass="button-primary"/>
            <Button fx:id="forwardButton" text="→ Chuyển tiếp" onAction="#onForward" styleClass="button-secondary"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Button fx:id="deleteButton" text="🗑 Xóa" onAction="#onDelete" styleClass="button-danger"/>
        </HBox>
    </bottom>
    
</BorderPane>
